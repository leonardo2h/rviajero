import React from 'react';
import { HeroStyle, InfoPage, Phrase } from './styles';

const HomeHero = () => (
  <HeroStyle>
    <InfoPage>
      <Phrase>
        <cite>"Los viajes se viven 3 veces: cuando los soñamos, cuando los vivimos, y cuando los recordamos."</cite>
      </Phrase>
    </InfoPage>
  </HeroStyle>
);

export default HomeHero;
